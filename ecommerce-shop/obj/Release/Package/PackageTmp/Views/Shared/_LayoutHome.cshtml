@using System.IdentityModel.Tokens.Jwt
@using System.Security.Claims
@using ecommerce_shop
@{
    var cookie = Request.Cookies["jwtToken"];
    bool isLoggedIn = false;
    string username = "";
    int userId = 0;

    if (cookie != null && !string.IsNullOrEmpty(cookie.Value))
    {
        var principal = JwtManager.GetPrincipal(cookie.Value);
        if (principal != null && principal.Identity != null && principal.Identity.IsAuthenticated)
        {
            isLoggedIn = true;
            username = principal.Identity.Name;

            var identity = (ClaimsIdentity)principal.Identity;
            var idClaim = identity.FindFirst(ClaimTypes.NameIdentifier);
            if (idClaim != null)
            {
                int.TryParse(idClaim.Value, out userId);
            }
        }
    }
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="~/Content/css/Style.css" rel="stylesheet" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
            <div class="container">
                <!-- Logo -->
                <a class="navbar-brand" href="@Url.Action("Index","Home")">
                    <strong>COCONUT</strong>
                    <p style="font-size: 0.8rem; margin-bottom: 0;">Cửa hàng trẻ em số 1 đông nam á</p>
                </a>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <!-- Giỏ hàng -->
                        <li class="nav-item">
                            <a class="nav-link" href="#"><img src="~/Content/imgs/icons8-shopping-cart-30.png" /></a>
                        </li>

                        <!-- Kiểm tra đăng nhập -->
                        @if (isLoggedIn)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown"
                                   role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <img src="~/Content/imgs/icons8-user-30.png" /> @username
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    <li>
                                        <a class="dropdown-item" href="@Url.Action("ViewProfile", "Profile", new { id = userId })">
                                            Thông tin
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider" /></li>
                                    <li>
                                        <a class="dropdown-item" href="@Url.Action("Logout","Account")">
                                            Đăng xuất
                                        </a>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Login","Account")">
                                    <img src="~/Content/imgs/icons8-user-30.png" /> Đăng nhập
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="@Url.Action("Register","Account")">
                                    Đăng ký
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>

        <!-- Thanh menu chính -->
        <nav class="navbar navbar-expand-lg" style="background-color: #dcc0a8;">
            <div class="container">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("Index","Home")">Trang chủ</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("ThoiTrang","Home")">Thời trang</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Phụ kiện</a></li>
                    <li class="nav-item"><a class="nav-link" href="@Url.Action("HuongDan","Home")">Hướng dẫn</a></li>
                </ul>
            </div>
        </nav>
    </header>

    <div class="container py-4">
        @RenderBody()
    </div>

    <!-- Footer -->
    <footer class="bg-light text-center text-lg-start">
        <div class="container p-4">
            <div class="row">
                <div class="col-lg-6 col-md-12 mb-4 mb-md-0 text-start">
                    <h5 class="text-uppercase">Về COCONUT</h5>
                    <p>
                        Chúng tôi tự hào là cửa hàng thời trang đẳng cấp,
                        phù hợp với các bé. Sản phẩm chất lượng...
                    </p>
                </div>
                <div class="col-lg-3 col-md-6 mb-4 mb-md-0 text-start">
                    <h5 class="text-uppercase">Chi nhánh</h5>
                    <ul class="list-unstyled mb-0">
                        <li><strong>Tiger City</strong></li>
                        <li>333, Heniken, Tiger City</li>
                        <li>102, Saranghae, Tiger City</li>
                        <li>ETC...</li>
                    </ul>
                </div>
                <div class="col-lg-3 col-md-6 mb-4 mb-md-0 text-start">
                    <h5 class="text-uppercase">Hỗ trợ</h5>
                    <p>Hotline: 0900 008888</p>
                    <p>Email: coconut.support@ezgg.com</p>
                </div>
            </div>
        </div>
        <div class="text-center p-3 bg-secondary text-white">
            © 2023 Copyright:
            <a class="text-white" href="#"> COCONUT</a>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>